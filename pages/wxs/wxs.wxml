<!--pages/wxs/wxs.wxml-->
<!-- wxs的定义方式 -->
<!-- 1.直接在wxml中定义 -->
<!-- <wxs module="info">
  //js代码
  var message = "Hello World";
  var name = "wbw";
  var sum = function(num1, num2) {
    return num1 + num2;
  }
  //commonjs的模块化导出的写法
  module.exports = {
    message: message,
    name: name,
    sum: sum
  }
</wxs> -->
<!-- 2.定义在单独的wxs文件中,再使用<wxs>标签进行导入 
      不能使用绝对路径,只能使用相对路径
-->
<wxs src="./info.wxs" module="info"></wxs>
<view>{{info.message}}</view>
<view>{{info.name}}</view>
<view>{{info.sum(20,30)}}</view>

<!-- 3.wxs的应用 -->
<view >{{info.price_format(price,1)}}</view>